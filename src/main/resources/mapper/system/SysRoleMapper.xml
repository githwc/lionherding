<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lh.system.mapper.SysRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lh.system.entity.SysRole">
        <id column="sys_role_id" property="sysRoleId" />
        <result column="role_name" property="roleName" />
        <result column="role_code" property="roleCode" />
        <result column="sort" property="sort" />
        <result column="remark" property="remark" />
        <result column="create_id" property="createId" />
        <result column="create_date" property="createDate" />
        <result column="update_id" property="updateId" />
        <result column="update_date" property="updateDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        sys_role_id, role_name, role_code, sort, remark, create_id, create_date, update_id, update_date
    </sql>

    <select id="getUserRoles" resultType="com.lh.system.entity.SysRole">
        select
            role_code
        from sys_role
        where sys_role_id in
            (select
                role_id
                from sys_user_role
                where user_id =
                (select sys_user_id from sys_user where login_name=#{loginName})
            )
    </select>
</mapper>
